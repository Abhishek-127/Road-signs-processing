CIS 4720 ASSIGNMENT 3
ROAD SIGN SEGMENTATION

--------------------------------------------------------
INSTRUCTIONS TO RUN THE CODE:
    PREREQUESITES:
    - HAVE PYTHON 2.7x installed on the machine
    - Have opencv version > 3.0.0 installed on the machine

RUN:
    - open terminal in the directory that has the algorithm.py code
    - python algorithm.py $PATH_TO_IMAGE
    - if you wish to include a video to process include it as the 2nd argument
    - python algorithm.py $PATH_TO_VIDEO video